# Contributing

欢迎提交问题、文档改进和代码补丁。

1. Fork 仓库并从 `main` 创建分支。
2. 不要提交 `.env`、运行状态、私人记忆、通知密钥或模型密钥。
3. 修改状态机时同时增加或更新测试。
4. 运行 `npm test`，确保全部测试通过。
5. Pull Request 请说明行为变化、兼容性与验证方式。

请把新的模型、记忆库和通知渠道实现为可选适配器，保持核心状态机可以离线运行。
