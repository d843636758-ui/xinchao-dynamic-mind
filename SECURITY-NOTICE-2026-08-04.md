# 心潮安全提醒：请检查你的 SERVICE_TOKEN

**2026-08-04**

如果你在 8 月 1 日之前部署过心潮，请花一分钟检查一件事。

## 问题

`.env.example` 里的 `SERVICE_TOKEN` 是一个占位值：`replace-with-a-random-secret`。

如果你照着示例部署、没有换掉它，而你的心潮又能从公网访问 —— 那么**任何读过这个开源仓库的人都知道你的 token**。

这个 token 的权限不小：

- 完整的 MCP 访问
- `/v1/dashboard/*`
- `/v1/state` —— 原始状态，包含你的驱动力数值、念头信号和梦境记录

## 谁受影响

需要同时满足三条：

1. 在 **2026-07-22（2.0 公开）到 2026-08-01** 之间部署
2. 没有换掉 `.env` 里的 `SERVICE_TOKEN`
3. 服务对公网可访问（只绑回环、只在内网跑的不受影响）

**2.3.4 及以后的版本在启动时就会拒绝这个占位值**，所以 8 月 1 日之后部署的不会中招。

## 怎么检查

在心潮目录下：

```bash
grep '^SERVICE_TOKEN=' .env
```

值以 `replace-with` 开头，或者长度不足 32 位 —— 两种都需要更换。

## 怎么修

```bash
openssl rand -hex 32
```

把生成的值填回 `.env` 的 `SERVICE_TOKEN=`，然后重启：

```bash
docker compose up -d
```

正常情况下这个 token 只存在于你自己的 `.env` 里，换掉不影响别的东西。如果你曾把它配置给其他客户端，记得一并更新。

顺便建议升级到最新版本。

## 想确认有没有被访问过

如果你的心潮前面有反向代理或隧道，可以翻一下访问日志，看 `/v1/state` 和 `/v1/dashboard/` 有没有来自陌生 IP 的 200 响应。没有日志也不用太紧张 —— 这是一个需要主动去找的洞，不是会被大规模扫描到的那种。

不放心的话，换掉 token 之后再看看驱动力数值和梦境记录有没有异常。

## 我们做了什么

这是默认值设计上的疏忽，责任在我们，不在照着文档做的你。

从 2.3.4 起，启动时会直接拒绝占位值和短于 32 位的 token —— 不会再有人在不知情的情况下带病运行。

给你添麻烦了，抱歉。
